{
  "configuration" : {
    "defaultCodes" : {
      "forwarding" : "auto neighbours = nodeState[\"neighbours\"]; std::vector<std::string> toSend = std::vector<std::string>(); if (neighbours.size() > 0) {int pos = rand() % neighbours.size(); toSend.push_back(neighbours[pos]);}return toSend;"
    },
    "logLevel" : ${log_level}
  },
  "state" : {
    "stop" : false,
    "changed" : false
  }
}