{
  "configuration" : {
    "defaultCodes" : {
      "forwarding" : "auto neighbours = nodeState[\"neighbours\"];std::vector<std::string> toSend = std::vector<std::string>();for (const std::string &n : neighbours) {if (n.compare(nodeState[\"from\"]) != 0) {toSend.push_back(n);}}return toSend;"
    },
    "logLevel" : ${log_level}
  },
  "state" : {
    "stop" : false,
    "changed" : false
  }
}